# 本项目一键验证命令 —— verify.sh / verify.ps1 共用的唯一事实源，由 /onboard 第 5 步填充。
# UNIX = macOS / Linux / Git Bash（bash 执行）    例: UNIX=mvn -q -DskipITs test
# WIN  = Windows（cmd.exe 执行）                  例: WIN=mvnw.cmd -q -DskipITs test
# 只做只读构建/测试：不改代码、不发布、不碰外部环境。
UNIX=
WIN=
