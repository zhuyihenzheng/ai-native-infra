# project/examples —（待对齐）

由 `/align-extract` 从**本项目真实代码**摘取的各层 golden example。AI 生成新代码时对齐这些样式，而非通用模板。

对齐后这里应有（按项目实际存在的层）：`controller`、`service-or-blogic`、`mapper-xml`、`entity-dto`、`exception`、`unit-test` 等各一份，外加 `bad-patterns.md`（不该模仿的旧写法）。

> 占位文件，对齐前为空。
