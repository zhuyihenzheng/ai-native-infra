# Aligned Rules —（待对齐）

> 状态：PLACEHOLDER —— 由 `/align-draft` 特化、`/align-review` 定稿后填充。
> 含未替换占位符或仍为 placeholder 时，`promote.sh` 会拒绝生效。

<!--
  对齐后本文件是「核心契约」，会被装配进根 CLAUDE.md / AGENTS.md / .github/copilot-instructions.md。
  结构建议（全部用本项目真实事实，每条标 [confirmed]/[assumed]）：

  ## 本项目是什么 / 技术栈
  ## 请求流（不可跳层）
  ## 各层硬约束（入口 / 业务 / Mapper / 实体）
  ## MyBatis 约束（#{} 强制、动态 SQL、分页、resultMap）
  ## 命名约定
  ## 禁止事项（forbidden）
  ## 命令（build / test / run）
  ## 安全（合成数据、禁真实 PII）
-->
